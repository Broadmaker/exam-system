# Vite will use this as the API base URL in production
# In dev, Vite proxies /api to localhost:8787 automatically
VITE_API_URL=

# Admin panel password (default: admin123)
VITE_ADMIN_PASSWORD=admin123
